# Shared Component Policy

组件通用性必须比较：

```text
语义作用
数据结构
操作集合
状态模型
信息层级
承载面约束
```

相同语义、数据、操作和状态模型应优先使用共用组件或 Variant。

仅视觉相似但语义、风险或行为不同的组件不得强行合并。

共用模式必须使用自然语言说明：

- 哪些页面使用；
- 哪些内容相同；
- 哪些差异通过 Variant 表达；
- 哪些差异要求独立组件；
- 为什么不合并外观相似但语义不同的对象。
