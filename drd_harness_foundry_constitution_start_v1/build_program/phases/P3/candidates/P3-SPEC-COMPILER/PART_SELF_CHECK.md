# P3-SPEC-COMPILER Self Check

- Scope is limited to `build_program/phases/P3/candidates/P3-SPEC-COMPILER/**`.
- The candidate defines `drd_compiler` only.
- All prior P3 review decisions are bound by sha256 and subject hash.
- Natural language remains the semantic authority; structured records are verification skeletons.
- The compiler is prohibited from semantic invention, product expansion, layout repair, copy rewriting, and direct source semantic reads outside the closed bundle.
- Input closure, deterministic order, hash drift, section refs, atomic semantic units, conservation, final manifest, hash indexes, reference indexes, and read-only QA boundaries are covered by validation rows.
- No repository code, root P3 files, prior P3 candidates, P1/P2 files, or control locks are modified by this candidate.
- No `P3_SPEC_LOCK` or `P3_BUILD_LOCK` is created.
- Open blockers: none.
