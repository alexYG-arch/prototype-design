# P2 Minimum Vertical Slice Scope

选择一个小型 PRD，至少包含：

- 一个用户任务；
- 一个真实页面；
- 一个系统 handoff 或 overlay；
- 所有页面可点击元素；
- 三至五个状态；
- 一个失败恢复；
- PRD 元素校验；
- 共用组件或表现模式的一次判定；
- 一份自然语言布局；
- 一份 `FINAL_DRD.md`。

必须证明从 Source Freeze 到 Final Review 的完整路径。
