# v3.1 Read-only Reference

The old PRD package is optional for the first constitution/spec workpack and required later for capability migration and adapter work.

Install it with `tooling/install_v31_baseline.py`. The extracted snapshot is read-only and must not be imported by the new DRD Harness.
