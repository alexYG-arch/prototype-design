# P4 Release Smoke Example

This fixture demonstrates a minimal governed release-smoke input bundle. It is
data for validation tests only; it does not create product semantics, publish a
package, or create locks.
